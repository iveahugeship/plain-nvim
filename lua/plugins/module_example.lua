-- Plugin module example.
-- Use this interface and require the module for include plugin.

local mod = { }

mod.meta = {
    name = '',
    source = '',
    description = '',
    dependencies = {}
}

mod.init = function()
end

return mod

